import { Canvas, Story } from '@storybook/blocks';

import * as ButtonStories from './components/Button/Button.stories';

import * as InputStories from './components/Input/Input.stories';


    # Button Component
    - Use buttons for the main actions on your page 
    - Identify the primary action and make it `primary`
    - [Go to Button documentation page](?path=/docs/demo-buttoncomponent--docs)
    ## Usage
    Primary Button
    <Canvas of={ButtonStories.Primary} />

    # Input Component
    - Use a input in your pages
    - Use disabled when it is readonly
    - [Go to Input documentation page](?path=/docs/demo-inputcomponent--docs)
    ## Usage
    Primary Input
    <Canvas of={InputStories.Primary} />


